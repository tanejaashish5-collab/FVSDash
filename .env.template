# ═══════════════════════════════════════════════════════════════
# FORGEVOICE STUDIO — Environment Variables Template
# Copy this to .env and fill in your values
# ALL variables with ★ REQUIRED must be set before the app works
# ═══════════════════════════════════════════════════════════════

# ── CORE (★ REQUIRED) ──────────────────────────────────────────
MONGO_URL=mongodb+srv://USERNAME:PASSWORD@cluster.mongodb.net/?retryWrites=true&w=majority
DB_NAME=forgevoice_prod
JWT_SECRET=REPLACE_WITH_RANDOM_64_CHAR_STRING
APP_ENV=production
CORS_ORIGINS=https://your-frontend.vercel.app

# ── AI / LLM (★ REQUIRED for content generation) ──────────────
# Primary: Google Gemini (free tier = 2M tokens/day)
# Get from: console.cloud.google.com → APIs → Credentials → API Key
# Enable: Gemini API, Vertex AI API
GEMINI_API_KEY=your-gemini-api-key-here

# Optional: for OpenAI/Claude model options in Strategy Lab
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...

# ── YOUTUBE (★ REQUIRED for analytics + publishing) ────────────
# Get from: console.cloud.google.com → Credentials → OAuth 2.0 Client
# Enable: YouTube Data API v3, YouTube Analytics API
YOUTUBE_CLIENT_ID=your-oauth-client-id.apps.googleusercontent.com
YOUTUBE_CLIENT_SECRET=GOCSPX-your-client-secret
YOUTUBE_API_KEY=AIza-your-api-key
# Set this AFTER you know your Railway URL:
YOUTUBE_REDIRECT_URI=https://your-backend.railway.app/api/oauth/callback/youtube
BACKEND_PUBLIC_URL=https://your-backend.railway.app

# ── ELEVENLABS (★ REQUIRED for voice generation) ───────────────
# Get from: elevenlabs.io → Profile → API Keys
# Free tier: 10,000 chars/month | Starter: $5/mo = 30k chars
ELEVENLABS_API_KEY=your-elevenlabs-api-key
# Optional: customize default voice (default = Rachel, professional female)
# ELEVENLABS_VOICE_ID=21m00Tcm4TlvDq8ikWAM

# ── GOOGLE VEO 2 (★ REQUIRED for video clip generation) ────────
# Same GCP project as YouTube — enable Vertex AI API
# Get from: console.cloud.google.com → Vertex AI → Enable API
# Then: IAM → Service Accounts → Create → Vertex AI User role → Key → JSON
# Set VEO_API_KEY = the service account key (or project API key with Vertex enabled)
VEO_API_KEY=your-veo-api-key-or-service-account-key

# ── STORAGE (optional — local /uploads folder used if not set) ──
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_S3_BUCKET=forgevoice-media
# AWS_REGION=ap-south-1
# AWS_S3_ENDPOINT_URL=   # Only for Cloudflare R2 / MinIO

# ── STRIPE (skip for internal use — Billing shows "Coming Soon") ─
# STRIPE_API_KEY=sk_live_...
# STRIPE_STARTER_PRICE_ID=price_...
# STRIPE_PRO_PRICE_ID=price_...
# STRIPE_ENTERPRISE_PRICE_ID=price_...

# ── TEMP STORAGE ────────────────────────────────────────────────
# Where FFmpeg stores intermediate video files during production
VIDEO_TEMP_DIR=/tmp/fvs_video
