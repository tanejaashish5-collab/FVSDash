# ForgeVoice Studio Frontend — Production Environment
# This file is committed to git. Secret values go in Vercel dashboard instead.

# Set this to your Railway backend URL in Vercel's environment variable settings
REACT_APP_BACKEND_URL=https://fvsdash-production.up.railway.app

# Build-time flags
REACT_APP_ENV=production
GENERATE_SOURCEMAP=false
